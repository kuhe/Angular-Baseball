/**
 * Enum for all baserunners.
 */
export type runner_name_t = 'batter' | on_base_runner_name_t;

/**
 * Enum for runners on base at the start of a play.
 */
export type on_base_runner_name_t = 'first' | 'second' | 'third';
